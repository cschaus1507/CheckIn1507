server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # React Router / SPA fallback:
    # If the request isn't a real file/folder, serve index.html and let the client router handle it.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
